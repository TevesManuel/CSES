/* AUTHOR: Manuel Tomas Teves */

#include <bits/stdc++.h>

#define dbg(x) std::cout << #x << " = " << (x) << ", "
#define dbgn(x) std::cout << #x << " = " << (x) << std::endl
#define lineN(n) for(int teves = 0; teves < n; teves++) std::cout << "="; std::cout << std::endl
#define line lineN(60)

typedef long long ll;
typedef long double ld;

const int inf = 1 << 30;
// const ll inf = 1LL << 60;

#define TEVES std::ios::sync_with_stdio(0);std::cin.tie(0);std::cout.tie(0);

int main() {
    TEVES

    int n, k;
    std::cin >> n >> k;

    dbg(n);
    dbgn(k);
    line;
    dbg(n);
    dbgn(k);

    return 0;
}